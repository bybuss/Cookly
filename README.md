В приложении испоьлзуется **Secrets Plugin** и для работы приложения необходимо [`local.properties`](local.properties) указать нужные данные: `BASE_API_URL`.
Пример файла [`local.properties`](local.properties): 
```.properties
sdk.dir=...
BASE_API_URL=https://example.api/
```
